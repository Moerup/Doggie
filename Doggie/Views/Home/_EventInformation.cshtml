@model Doggie.Models.Event
@{
    ViewBag.Title = "Contact";
}

<div class="panel panel-info">
    <div class="panel-heading">
        <h3 class="panel-title">@Model.Location</h3>
    </div>
    <div class="panel-body">
        <p>Forventet tid: @Model.EstimatedTimeMinutes min</p>
        <p>Start tidspunkt: @Model.StartingTime.TimeOfDay.ToString("hh\\:mm")</p>
    </div>
</div>